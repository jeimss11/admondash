import { Routes } from '@angular/router';
import { DistributorsComponent } from './distributors/distributors.component';

export const routes: Routes = [{ path: '', component: DistributorsComponent }];
